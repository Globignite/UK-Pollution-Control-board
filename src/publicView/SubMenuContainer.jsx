import { Box, Typography } from '@mui/material';

const SubMenuContainer = (props) => {
  return (
    <Box padding={2}>
      <Typography variant="h5" gutterBottom>
        this is the sub menu  container 
      </Typography>
      <Typography variant="h5" gutterBottom>
        this is the sub menu  container 2
      </Typography>
     
    </Box>
  );
};

export default SubMenuContainer;
